# Install Penguin 3

To install Penguin 3, you can double-click `penguin_3_whole_install.bat` (or `penguin_3_whole_install` if extensions are hidden).

!!! info
    To only install required dependencies, click `install.bat`.